---
import '../styles/global.css';
const { titre = 'Recettes Meal Prep' } = Astro.props;
---
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Carnet de recettes meal prep — déficit calorique et maintien musculaire." />
    <title>{titre}</title>
    <link rel="icon" href={`data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🍱</text></svg>`} />
  </head>
  <body>
    <slot />
    <footer class="pied">
      <div class="wrap">Carnet meal prep · mis à jour {new Date().toLocaleDateString('fr-CA')}</div>
    </footer>
  </body>
</html>
